async function run(sock, from, msg) {
    // Type your code that runs on every message in here
    // Example:
    // console.log("(Raw) Message recieved - " + msg.text)
}

module.exports = {
    run: run,
}